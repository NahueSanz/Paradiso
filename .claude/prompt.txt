You are working on a React + Vite + TypeScript project.

We already have two SVG assets:
- /public/logo-full.svg
- /public/logo-icon.svg

And a background image:
- public/paddle-linea-blanca.png

Your task is to integrate them properly into the app.

-------------------------------------
1) FAVICON (Browser Tab Icon)
-------------------------------------

Replace the default Vite favicon with the custom icon.

Steps:
- Use /logo-icon.svg as the favicon
- Update index.html

Implementation details:
- Locate index.html in the root
- Replace the existing <link rel="icon"> with:

<link rel="icon" type="image/svg+xml" href="/logo-icon.svg" />

Make sure it works in the browser tab.

-------------------------------------
2) LOGIN PAGE LOGO
-------------------------------------

Improve the login/register screen by adding branding.

Requirements:
- Add /logo-full.svg at the top of the login form
- Center it horizontally
- Give it proper spacing (margin-bottom)
- Keep the existing layout (do NOT break current styling)

Example:
- Logo above the title or form
- Size: around height 40–60px

-------------------------------------
3) BACKGROUND IMAGE (PADEL COURT)
-------------------------------------

Enhance the login page visually.

Requirements:
- Use /paddle-linea-blanca.png
- The image should be subtle (not distracting)

Implementation options:
- Use a div with background-image OR Tailwind classes

Style requirements:
- Cover the entire screen (background-size: cover)
- Centered (background-position: center)
- Add a dark overlay for readability (important)

Example approach:
- Wrapper div with:
  - background image
  - semi-transparent dark overlay (e.g. bg-black/60)

-------------------------------------
4) UX QUALITY RULES
-------------------------------------

- Do NOT break responsiveness
- Do NOT make text hard to read
- Keep contrast high
- Maintain a clean SaaS look (not flashy)

-------------------------------------
5) BONUS (optional but recommended)
-------------------------------------

- Add a slight blur or overlay to improve contrast
- Ensure the logo looks good in both light/dark mode

-------------------------------------

Output:
- Show the updated index.html
- Show the updated Login/Register component
- Do not change unrelated code