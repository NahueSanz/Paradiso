You are working on a full-stack TypeScript app (React + Node + Prisma).

We need to fully implement **client phone number support for reservations** (both regular and fixed reservations).

This must be consistent across:

* Database (Prisma)
* Backend services/controllers
* Frontend forms (ReservationModal and FixedReservationModal)
* UI display

---

## 1. DATABASE (Prisma)

Update both models:

model Reservation {
...
clientPhone String?  // optional but recommended
}

model FixedReservation {
...
clientPhone String?
}

* Run migration
* Ensure existing data is not broken

---

## 2. BACKEND

### Controllers:

* Accept `clientPhone` in:

  * createReservation
  * updateReservation
  * createFixedReservation
  * updateFixedReservation

Validate:

* Optional field
* If present → must be a valid phone format (basic validation is fine)

Example:

* Allow digits, spaces, +, -
* Reject empty string → store as null

---

### Services:

* Persist `clientPhone` correctly
* Include it in all returned objects

---

## 3. FRONTEND — FORMS

### ReservationModal.tsx

### FixedReservationModal.tsx

Add a new input:

Label:
"Teléfono (opcional)"

Input:

* type="tel"
* placeholder="11 1234-5678"

State:
const [clientPhone, setClientPhone] = useState('')

Validation:

* Optional
* If filled → basic format check

---

### IMPORTANT:

Include it in payloads:

createReservation({
...
clientPhone: clientPhone.trim() || undefined
})

Same for:

* updateReservation
* createFixedReservation
* updateFixedReservation

---

## 4. UI DISPLAY

In schedule grid / reservation cards:

Show phone under client name:

Juan Pérez
📞 11 1234-5678

* Only render if exists
* Use subtle styling (text-xs, muted color)

---

## 5. EDIT MODE

When editing a reservation:

* Pre-fill phone number correctly

---

## 6. OPTIONAL UX IMPROVEMENT (VERY NICE)

Make phone clickable:

<a href={`https://wa.me/54${phoneSanitized}`}>
📞 {clientPhone} </a>

* Strip spaces/dashes
* Assume Argentina (+54)

---

## 7. EDGE CASES

* Empty string → store null
* Editing → allow clearing the phone
* Do NOT break existing reservations

---

## 8. FINAL CHECK

Verify:

* Create reservation with phone ✅
* Edit reservation updates phone ✅
* Fixed reservations support phone ✅
* Phone shows in UI ✅

---

Keep styling consistent with the existing dark mode system.
